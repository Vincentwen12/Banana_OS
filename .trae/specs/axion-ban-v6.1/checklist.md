# Checklist: Axion-Ban v6.1

## W1: 引导 + VGA + 键盘 + 基础 Shell + Ω位图 + 调度骨架

### 编译
- [ ] `make` 无警告，生成 `axion.iso`
- [ ] `make debug` 生成带调试符号的内核
- [ ] `make clean` 清理所有构建产物

### 启动
- [ ] `make run` 启动到 Shell 提示符 `>`，总时间 < 2s
- [ ] 串口输出 "Axion-Ban Kernel v0.1" 启动 Banner
- [ ] 串口输出启动耗时（毫秒）
- [ ] Multiboot 引导链无错误（GRUB → 32-bit → 长模式 → kmain）

### VGA 显示
- [ ] 启动 Banner 完整显示在 VGA 屏幕
- [ ] 彩色文本正确渲染
- [ ] 屏幕滚动正常（超过 25 行后自动上滚）
- [ ] `clear` 命令清空屏幕

### 键盘输入
- [ ] 键盘输入字符实时回显到 VGA 屏幕
- [ ] 退格键正确删除字符
- [ ] 回车键正确提交命令
- [ ] Shift 键正确切换大小写
- [ ] 连续快速按 1000 次键，所有字符显示正确，系统不崩溃

### Shell 命令
- [ ] `help` 命令列出所有可用命令（9 个）
- [ ] `hello` 命令输出 "Hello, world!"
- [ ] `clear` 命令清空屏幕
- [ ] `reboot` 命令成功复位系统
- [ ] `panic` 命令显示 `[PANIC] Manual panic` 并暂停
- [ ] 未知命令显示错误提示

### 内存管理
- [ ] `mem` 命令显示物理内存总量、已用页、空闲页、等效容量
- [ ] `alloc 10` 分配成功，mem 显示已用页增加 10
- [ ] `free 10` 释放成功，mem 显示已用页减少 10
- [ ] `alloc 999999` 触发 OOM，显示 `[PANIC] Out of memory`
- [ ] 多次 alloc/free 循环后内存统计正确（无泄漏）

### 调度器
- [ ] 调度器骨架编译通过
- [ ] ready_mask 位操作正确

### 门铃 IPC
- [ ] 门铃骨架编译通过
- [ ] 数据结构缓存行对齐

### 错误处理
- [ ] kernel_panic 正确显示错误信息
- [ ] panic 后系统停止（HLT 循环）

### 代码质量
- [ ] 内核二进制 < 80 KB
- [ ] 无编译警告（`-Wall`）
- [ ] 所有 .h 文件有 include guard

## W2: Ω 弹性内存（完整版）

- [ ] SWAR 热度在 pmalloc 中正确更新
- [ ] SWAR 热度在 pfree 中正确重置
- [ ] hotness_age() 全局老化正确
- [ ] XOR 压缩/解压 4KB 页面数据往返一致
- [ ] 回旋镖池三级冷数据正确下沉
- [ ] `mem` 显示等效容量 ≈ 物理 × 2.3
- [ ] alloc/free 压力测试（1000 次循环）通过

## W3: Ψ 调度器 + Δ 门铃 IPC（完整版）

- [ ] 5 级 MLFQ 优先级队列正确维护
- [ ] 后台 CPU 满载时，前台 Shell 输入延迟 < 10ms
- [ ] 低优先级任务经老化后获得 CPU 时间
- [ ] 上下文切换正确保存/恢复寄存器
- [ ] 多核 AP 成功启动
- [ ] 核间门铃通信正常
- [ ] 门铃 ACL 拒绝未授权通道写入

## W4: Σ 功耗管家

- [ ] 空载时 CPU 执行 HLT，QEMU 主机侧 CPU < 5%
- [ ] 低负载时切换为轮询模式
- [ ] 高负载时切换为中断模式
- [ ] CPU 频率根据负载自动调节

## W5-W6: Linux ABI 兼容层

- [ ] 静态编译的 bash 正常运行
- [ ] 动态链接的 bash 正常运行
- [ ] gcc 可编译简单 C 程序
- [ ] python 可执行脚本
- [ ] /proc 文件系统可访问
- [ ] 约 150 个系统调用全部实现

## W7: 文件系统

- [ ] EXT4 分区可挂载
- [ ] 文件列表可读取
- [ ] 文件内容可读写
- [ ] AxeFS 创建/读写/删除文件正常
- [ ] 文件系统崩溃后一致性检查通过

## W8-W9: 自研桌面环境

- [ ] 窗口管理器可创建/拖拽/关闭窗口
- [ ] 文件管理器可浏览目录树
- [ ] 终端模拟器可执行 Shell 命令
- [ ] 包管理器可安装/卸载软件
- [ ] 设置中心可修改系统配置

## W10: 交付与部署

- [ ] ISO 可启动进入 Live 桌面
- [ ] 一键安装器可完成完整安装
- [ ] 安装后系统可独立启动
- [ ] 24h 稳定性压测通过（无崩溃、无内存泄漏）